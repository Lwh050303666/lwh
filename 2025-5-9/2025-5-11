今天是2025年5月11日，我的算法书籍<<代码随想录>>前几天到货了，这几天一直在跟着这本书练习算法，这本书感觉写得很好，很符合一般小白的思路，平时在网上看解析的时候哪怕有很详细的注释也很难全看懂，因为那些人大多不是算法新手，相信跟着这本书坚持练下去的话会有很好的效果。
这几天开了linux，学了很多有关linux的指令，下面来复习一下这几天学习的linux指令
ALT+ENTER 是对linux大小屏
ls用来查看当前目录下的文件，ls -l（简写为ll）显式文件更多属性   ls -a显示所有的文件，包括隐藏文件。    ls -d 显示目录的自身信息，而不是它内部文件的信息
pwd显示当前命令行在操作时位于哪个目录
mkdir file 是创建目录    rm可以删除任意类型的文件，包括普通文件和目录文件    在linux中 -r 对目录相关的操作来讲就是操作这个目录下所有的文件，可以把-r理解为递归  rm -rf可递归并强制删除文件
touch 建立普通文本文件
adduser name是新建用户   passwd name 给用户设置密码     userdel -r name 删除账号
在文件有关的信息中，第一个字母表示文件类型 d:目录 -:普通文件   在linux中，以“.”开头的文件叫隐形文件
cd是改变目录的命令     cd ~ 可以直接进入家目录     cd-回退到最近一次所在的路径     
"."表示当前路径  ".."表示当前路径的上级路径
linux的目录是一种多叉树  tree命令将对应的目录结构以树状结构显示  tree -p创建一串路径
把从根节点开始定位到指定位置且有唯一性的路径称之为指定路径    配置文件用绝对路径   一般命令行输入用相对路径
whoami 查看当前用户
/home/lwh 是用户的家目录，centos系统中，所有的普通用户名字都会放在该目录下


"|"是一种内存级的管道文件     *是通配符，表示任何一个文件
man用来查看指令对应的说明  
echo "str" 把字符串内容打印出来，默认打印到屏幕上   echo "str" test.c 表示向test.c中写入str
cat 打印文件内容     cp -f 强行复制文件或目录，无论无的文件或目录是否已经存在   cat < 本来应从键盘读取，现在是从指定文件读取（输入重定向）  cat -n 对输出的所有行编号 
ctrl+c 终止因为程序/指令异常而无法进行指令输入
alias A=B(起别名，而且是临时的，不会永久改变配置)    
>向目标文件写入时会清空文件并写入新内容  >表示清空重定向  >>表示追加重定向


